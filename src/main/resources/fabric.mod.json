{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "PolyCOre - A core library for PolyCube mods.",
  "authors": [
    "RenardElectric"
  ],
  "contact": {},
  "license": "MIT",
  "icon": "assets/polycore/icon.png",
  "environment": "server",
  "depends": {
    "java": ">=${java_version}",
    "minecraft": "${minecraft_version}",
    "fabricloader": ">=${loader_version}",
    "fabric-api": ">=${fabric_api_version}"
  }
}
